#include <iostream>
using namespace std;

int main() {
	
	float cm, m, km;
	scanf ("%f", &cm);
	scanf ("%f", &m);
	scanf ("%f", &km);
	printf ("convert centimeter into meter");
	printf ("%f", m=cm/100.0);
	printf ("convet crntimeter into kilomter");
	printf ("%f", km=cm/100000.0);
	 
	return 0;
}
