#include <iostream>
using namespace std;

int main() {
	
	int d, y, w, m;
	scanf ("%d", &d);
	scanf ("%d", &y);
	scanf ("%d", &w);
	scanf ("%d", &m);
	printf ("no. of days into years ");
	printf ("%d", y=d/365);
	printf ("no. of days into weeks ");
	printf ("%d", w=d/7);
	printf ("no. of days into days ");
	printf ("%d", d=d);
        printf ("no. of days into months");
        printf ("%d", m=d/30);
	 
	return 0;
}
