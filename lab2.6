#include <iostream>
#include <cmath>
using namespace std;

int main() {
	
	float x, y;
	scanf ("%f", &x);
	scanf ("%f", &y);
	printf ("value of root over x=");
	printf ("%f", y=pow(x,0.5));

	return 0;
}
